funtion convertFahrToCelsius(f){  
prompt('Enter your temperature in Fahr');
console.log(prompt);
`${parameter} is not a valid number but a/an ${parameter's type}.`
}
const cel = (f-32) * 5/9
console.log(cel);


function checkYuGiOh(){
  prompt('Enter an integer of your choice:');
  console.log(prompt)

const N = [];
const arr = Array.from({length: N}, (_, index) => index + 1);
console.log(arr);
 } 
